import { BoardType } from "@/common/types/BoardType";
const postBoardApi: (data: BoardType, boardId: number) => boolean = (
  data,
  boardId
) => {
  // post board data to backend
  return true;
};

export default postBoardApi;